name: Check npm outdated CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [8.x]

    steps:
    - name: npm outdated
      run:
        npm outdated
      env:
        CI: true

